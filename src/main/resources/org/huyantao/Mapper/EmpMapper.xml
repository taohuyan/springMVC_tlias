<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.huyantao.Mapper.EmpMapper">
    <insert id="Insert" parameterType="org.huyantao.pojo.Emp">
        insert into emp(username,name,gender,image,dept_id,entrydate,job,create_time,update_time)
                    values(#{username},#{name},#{gender},#{image},#{deptId},#{entrydate},#{job},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        update emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="entrydate != and entrydate != ''">entrydate = #{entrydate},</if>
            <if test="job != null">job = #{job},</if>
            <if test="update_time != null">update_time = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>

    <select id="count" resultType="java.lang.Long">
        select count(*) from emp;
    </select>
    <select id="QueryByPage" resultType="org.huyantao.pojo.Emp">
        select * from emp
        <where>
            <if test="name != null">
                name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and entrydate between #{begin} and #{end}
            </if>
        </where>
        order by update_time desc
        limit #{page},#{pageSize}
    </select>


    <delete id="deleteByIds">
        delete from emp where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>